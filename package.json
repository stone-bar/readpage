{
  "name": "readpage",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "npm run generate:articles && vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "generate:articles": "node scripts/generate-articles.mjs",
    "convert:txt": "node scripts/convert-txt.mjs"
  },
  "devDependencies": {
    "vitepress": "1.6.4"
  }
}